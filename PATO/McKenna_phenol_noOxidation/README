gmshToFoam mesh.msh
-> update constant/boundary with "wedge" in the respective places
    - a_front, a_back
    - f_front, f_back
    - g_front, g_back
    - s_front, s_back
splitMeshRegions -cellZones -overwrite
sh setFields.sh
PATOx
